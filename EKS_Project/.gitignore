# Local .terraform directories
**/.terraform/*

# .tfstate files (CRITICAL: 절대 커밋 금지)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# (변수 정의 파일, 민감 정보 포함 가능성 높음)
*.tfvars
*.tfvars.json

# Ignore override files as they are usually local overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc